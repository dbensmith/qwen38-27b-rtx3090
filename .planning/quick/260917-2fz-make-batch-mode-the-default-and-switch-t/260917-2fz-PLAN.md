---
id: 260917-2fz
slug: make-batch-mode-the-default-and-switch-t
description: make batch mode the default and switch to it now.
mode: quick
created: 2026-09-17T07:45:35.393Z
---

# Quick Task Plan: Make batch mode the default and switch to it now

## Task
Make batch mode the default profile and switch to it now.

## Plan
1. Update `switch.sh` to default to `batch` instead of `single` when no profile is running and no `.current-profile` exists
2. Run `./switch.sh batch` to switch to batch mode now

## Files to Modify
- `switch.sh` - Change default from `single` to `batch` in the no-argument resolution logic

## Verification
- Verify the change in `switch.sh` is correct
- Run `./switch.sh batch` and confirm it switches to batch mode
- Check that `.current-profile` contains `batch`