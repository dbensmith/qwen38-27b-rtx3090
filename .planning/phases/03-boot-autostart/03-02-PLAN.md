---
phase: 03-boot-autostart
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/PROJECT.md
  - switch.sh
  - .planning/state.json
  - .planning/quick/260917-2fz-make-batch-mode-the-default-and-switch-t/260917-2fz-SUMMARY.md
autonomous: true
requirements: [BOOT-02]

estimate:
  tokens: 8000
  raw_tokens: 8000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md BOOT-02 (line 24) and Core Value (line 4) state the boot-hook default as `batch`, matching the locked default (D-08) rather than the stale `single`."
    - "ROADMAP.md Phase 3 goal (line 39) and success criterion 3 (line 45) state the no-persisted-state default as `batch`."
    - "PROJECT.md Key Decisions default-profile row (line 64) reads `Default profile is batch on first run` with the quick-task rationale and a Good outcome marker."
    - "PROJECT.md source text (not mirrors) is aligned too: the Core Value sentence (lines 18-20) reads `with batch as the safe default` and the active-requirement line (line 35) reads `defaulting to `batch` on first run` — PROJECT.md is the source the GSD mirrors resync from, which is exactly why the mirrors themselves stay untouched (checker WARNING-1 correction: lines 18-20/35 were mislabeled as GSD mirrors)."
    - "switch.sh's header comment (line 12) says `absent/blank/corrupt -> batch`, matching its own code at line 109."
    - "The do-not-touch items are byte-identical to before: REQUIREMENTS SWITCH-03 (line 18, still `single`), the switch.sh inline comment (lines 97-100), the real GSD auto-mirrors (STATE.md:25 and .claude/CLAUDE.md, which resync from PROJECT.md on the next GSD state update and are never hand-edited), and .gsd/."
    - "The two qwen-repo commits are clean: the phase-2 bookkeeping commit stages only the pending Phase-2 files, and the D-10 commit stages only the four aligned doc files — no .gsd/ or .current-profile in either (each commit carries a negative assertion in its task's verify, checker WARNING-2)."
  artifacts:
    - ".planning/REQUIREMENTS.md (BOOT-02 + Core Value now `batch`; the SWITCH-01..04 [x] completion markings are committed in the phase-2 commit)"
    - ".planning/ROADMAP.md (Phase 3 goal + criterion 3 now `batch`)"
    - ".planning/PROJECT.md (default-profile row now `batch` / Good; Core Value sentence and active-requirement line now `batch` — source text aligned, D-08/D-10)"
    - "switch.sh (header comment line 12 now `-> batch`)"
    - "Two qwen-repo commits: `docs(02): record phase 2 completion` and `docs(03): align default-profile wording (D-08/D-10)`"
  key_links:
    - "D-08 (locked default `batch`) makes the four doc locations agree with switch.sh:109's actual code default (the D-10 consistency link)."
  prohibitions:
    - "MUST NOT edit the do-not-touch items: REQUIREMENTS SWITCH-03 (line 18, a completed [x] historical requirement that still says `single`), the switch.sh inline comment (lines 97-100), the real GSD auto-mirrors (STATE.md:25 and .claude/CLAUDE.md — they resync from PROJECT.md automatically and must never be hand-edited), or anything under .gsd/. (The PROJECT.md Core Value / active-requirement lines are source text, not mirrors, and ARE part of the D-10 edit set — checker WARNING-1 correction.)"
    - "MUST NOT stage or commit .gsd/ or the qwen repo's .current-profile, or any file outside the two named commits; MUST NOT push."
    - "MUST NOT change switch.sh's code — only its header comment at line 12; D-10 is doc alignment, not behavior."
---

<objective>
Align the planning docs with the locked default so the recorded default matches what `switch.sh:109` actually does: the no-persisted-state fallback is `batch` (D-08), not the stale `single` wording that survived the quick task 260917-2fz. Per D-10 this covers BOOT-02's wording, ROADMAP Phase 3's goal + criterion 3, and PROJECT's Key-Decision row, plus four agent-discretion extras: REQUIREMENTS Core Value, the switch.sh header comment, and — corrected by the checker's WARNING-1 — PROJECT.md's own Core Value + active-requirement source text (source the GSD mirrors resync from, not mirrors themselves). It also commits the still-pending Phase 2 bookkeeping (the SWITCH-01..04 completion markings, the phase-2 ROADMAP status, state.json, and the quick-task SUMMARY) so the milestone state is consistent before the Phase 3 boot work.

Purpose: D-08 is a locked decision; the docs currently contradict it, which is exactly the kind of stale state that misleads a future reader or verifier. This plan closes that gap — and records the pending Phase 2 completion — without touching any behavior.

Output: four doc files aligned to `batch` (eight locations across them), two clean qwen-repo commits (phase-2 bookkeeping, then D-10 alignment) whose scope is negatively verified (no .gsd/ or .current-profile in either), and a consistent GSD milestone state.
</objective>

<execution_context>
@/home/pengwin/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/pengwin/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-boot-autostart/03-CONTEXT.md
@.planning/phases/03-boot-autostart/03-RESEARCH.md
@.planning/phases/03-boot-autostart/03-PATTERNS.md
@switch.sh
</context>

## Artifacts This Phase Produces

- Aligned planning docs: `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, `switch.sh` (all now state the `batch` default, matching switch.sh:109).
- Two clean qwen-repo commits: `docs(02): record phase 2 completion` (the pending Phase-2 bookkeeping) and `docs(03): align default-profile wording (D-08/D-10)` (the four aligned doc files).
- A consistent GSD milestone state (state.json + quick-task SUMMARY committed).

<tasks>

<task type="auto">
  <name>Task 1: Commit the still-pending Phase 2 bookkeeping (SWITCH completion, phase-2 roadmap status, state.json, quick SUMMARY)</name>
  <read_first>
    - the output of `git -C /home/pengwin/repos/qwen38-27b-rtx3090 status --porcelain` and `git -C /home/pengwin/repos/qwen38-27b-rtx3090 diff .planning/REQUIREMENTS.md .planning/state.json` (to see exactly which Phase-2 bookkeeping is pending)
    - .planning/quick/260917-2fz-make-batch-mode-the-default-and-switch-t/260917-2fz-SUMMARY.md (the quick-task record being committed)
  </read_first>
  <files>
    .planning/REQUIREMENTS.md
    .planning/state.json
    .planning/quick/260917-2fz-make-batch-mode-the-default-and-switch-t/260917-2fz-SUMMARY.md
  </files>
  <action>
    The Phase 2 bookkeeping is already pending in the working tree (a prior GSD run left it uncommitted): `.planning/REQUIREMENTS.md` has the SWITCH-01..04 checkboxes flipped from unchecked to checked and the tracking-table status flipped from Pending to Complete; `.planning/state.json` has the phase-2-complete transition; and the quick-task SUMMARY is modified. Commit these as-is with no new edits. Stage ONLY these three files (`.planning/REQUIREMENTS.md`, `.planning/state.json`, the quick SUMMARY) — do NOT stage `.planning/ROADMAP.md` (its phase-2 status change was already recorded in the planner's "create phase plans" commit) and do NOT stage `.gsd/` or `.current-profile`. Commit with the subject `docs(02): record phase 2 completion`. This must happen BEFORE any D-10 edit so the D-10 commit (Task 2) carries only the default-alignment diff.
  </action>
  <acceptance_criteria>
    - A commit with subject `docs(02): record phase 2 completion` exists and its `git show --stat` lists exactly the three Phase-2 bookkeeping files (REQUIREMENTS, state.json, quick SUMMARY).
    - `.gsd/` and `.current-profile` are not in the commit; nothing is pushed.
  </acceptance_criteria>
  <verify>
    <automated>git -C /home/pengwin/repos/qwen38-27b-rtx3090 log --oneline -1 | grep -qF 'record phase 2 completion' &amp;&amp; echo PASS</automated>
    <fails_when>The phase-2 bookkeeping commit is missing or has a different subject.</fails_when>
    <automated>git -C /home/pengwin/repos/qwen38-27b-rtx3090 show --stat HEAD | grep -qF 'REQUIREMENTS.md' &amp;&amp; git -C /home/pengwin/repos/qwen38-27b-rtx3090 show --stat HEAD | grep -qF 'state.json' &amp;&amp; git -C /home/pengwin/repos/qwen38-27b-rtx3090 show --stat HEAD | grep -qF '260917-2fz-SUMMARY.md' &amp;&amp; echo PASS</automated>
    <fails_when>Commit A does not contain the Phase-2 bookkeeping files, or it contains unexpected files (ROADMAP, .gsd, or .current-profile).</fails_when>
    <automated>! git -C /home/pengwin/repos/qwen38-27b-rtx3090 show --stat HEAD | grep -qE '\.gsd/|\.current-profile' &amp;&amp; echo PASS</automated>
    <fails_when>The phase-2 commit stages a forbidden file (.gsd/ or .current-profile) — the negative assertion (WARNING-2) failed.</fails_when>
  </verify>
  <done>The still-pending Phase 2 bookkeeping (SWITCH-01..04 completion, phase-2 roadmap status, state.json transition, quick SUMMARY) is committed as `docs(02): record phase 2 completion`, staging only those files; .gsd/ and .current-profile are untouched and nothing is pushed.</done>
</task>

<task type="auto">
  <name>Task 2: Align the eight doc locations to the locked `batch` default (D-08/D-10) and commit them as the D-10 commit</name>
  <read_first>
    - .planning/phases/03-boot-autostart/03-RESEARCH.md (lines 449-473: the exact D-10 before/after diffs)
    - .planning/phases/03-boot-autostart/03-PATTERNS.md (the before/after strings)
    - switch.sh (line 12 header comment vs line 109 code — the code already says `batch`; only the header comment is stale)
  </read_first>
  <files>
    .planning/REQUIREMENTS.md
    .planning/ROADMAP.md
    .planning/PROJECT.md
    switch.sh
  </files>
  <action>
    Apply exactly these eight edits, each a small single-token or single-phrase change (plus the rationale for the PROJECT row). Before each edit, confirm the before-string is present verbatim; if any before-string is missing, STOP and report it rather than guessing (a missing before-string means the line already changed or the number drifted).
    (1) .planning/REQUIREMENTS.md line 4 (Core Value): in the Core Value sentence, change `with single-user as the safe default` to `with batch as the safe default` (leave the trailing `until a mode is explicitly chosen.` intact).
    (2) .planning/REQUIREMENTS.md line 24 (BOOT-02): change the line `If no profile was ever selected, the boot hook starts ` + backticked `single` + ` by default` to the same with backticked `batch` (i.e. the backticked `single` in the BOOT-02 line becomes backticked `batch`; do NOT touch the Core Value line 4, which uses the un-backticked `single-user`).
    (3) .planning/ROADMAP.md line 39 (Phase 3 Goal): in the Goal sentence, change `defaulting to ` + backticked `single` + ` if nothing was ever chosen` to the same with backticked `batch`.
    (4) .planning/ROADMAP.md line 45 (Phase 3 success criterion 3): change `On a machine with no persisted state yet, the boot hook starts ` + backticked `single` to the same ending with backticked `batch`.
    (5) .planning/PROJECT.md line 64 (Key Decisions default-profile row): replace the whole row `| Default profile is ` + backticked `single` + ` on first run | Matches "single user fast mode as default" | — Pending |` with `| Default profile is ` + backticked `batch` + ` on first run | Quick task 260917-2fz (2026-09-17) made batch the current default; boot follows the same fallback (switch.sh:109) |` + the Good outcome marker the neighboring row (line 62) uses.
    (6) switch.sh line 12 (header comment only): change `absent/blank/corrupt -&gt; single` to `absent/blank/corrupt -&gt; batch`. Do NOT touch the inline comment at lines 97-100 (a stale SWITCH-03 reference, explicitly out of D-10 scope) and do NOT touch any switch.sh code (line 109 already says `batch`).
    (7) .planning/PROJECT.md lines 18-20 (Core Value sentence — source text, NOT a mirror: this is the text the GSD mirrors in STATE.md:25 and .claude/CLAUDE.md resync from, which is exactly why the mirrors must not be hand-edited): change `with single-user as the safe default` to `with batch as the safe default`, leaving the rest of the sentence intact.
    (8) .planning/PROJECT.md line 35 (active-requirement line — also source text): change `defaulting to `single` on first run` to `defaulting to `batch` on first run` (backticked token only).
    Leave every do-not-touch item byte-identical: REQUIREMENTS line 18 (SWITCH-03, a completed historical requirement that still says `single`), the switch.sh inline comment (lines 97-100), the real GSD auto-mirrors (STATE.md:25 and .claude/CLAUDE.md — they resync from PROJECT.md on the next GSD state update; hand-editing them would diverge them from the source), and anything under .gsd/.
    Then stage ONLY these four files (`.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, `.planning/PROJECT.md`, `switch.sh`) and commit with the subject `docs(03): align default-profile wording (D-08/D-10)`. Do NOT stage .gsd/, .current-profile, or anything else (the negative `git show --stat` check in &lt;verify&gt; enforces this); do NOT push.
  </action>
  <acceptance_criteria>
    - All eight after-strings are present in their files; the four files' D-10-targeted `single` references now read `batch`, matching switch.sh:109 (this includes PROJECT.md's Core Value + active-requirement source text, from which the GSD mirrors resync).
    - The do-not-touch items are unchanged (REQUIREMENTS SWITCH-03 line 18 still says `single`; the switch.sh inline comment lines 97-100 are intact; STATE.md and .claude/CLAUDE.md are absent from the D-10 commit's diff).
    - The D-10 commit exists, its `git show --stat` lists exactly the four doc files, and .gsd/ + .current-profile are absent from it (negative-checked).
  </acceptance_criteria>
  <verify>
    <automated>grep -qF 'starts batch by default' .planning/REQUIREMENTS.md &amp;&amp; grep -qF 'with batch as the safe default' .planning/REQUIREMENTS.md &amp;&amp; echo PASS</automated>
    <fails_when>REQUIREMENTS BOOT-02 or the Core Value still says `single` (D-08 not reflected in the requirement).</fails_when>
    <automated>grep -qF 'defaulting to batch' .planning/ROADMAP.md &amp;&amp; grep -qF 'the boot hook starts batch' .planning/ROADMAP.md &amp;&amp; echo PASS</automated>
    <fails_when>ROADMAP Phase 3 goal or success criterion 3 still says `single`.</fails_when>
    <automated>grep -qF 'Default profile is batch on first run' .planning/PROJECT.md &amp;&amp; grep -qF 'corrupt -&gt; batch' switch.sh &amp;&amp; echo PASS</automated>
    <fails_when>PROJECT default-profile row or the switch.sh header comment still says `single`.</fails_when>
    <automated>grep -qF 'with batch as the safe default' .planning/PROJECT.md &amp;&amp; grep -qF 'defaulting to `batch` on first run' .planning/PROJECT.md &amp;&amp; echo PASS</automated>
    <fails_when>PROJECT.md's own Core Value (lines 18-20) or active-requirement line (line 35) still says `single` — the source text the GSD mirrors resync from (WARNING-1 correction: these are source, not mirrors, so they ARE in the D-10 edit set).</fails_when>
    <automated>grep -qF 'defaults to `single`' .planning/REQUIREMENTS.md &amp;&amp; grep -qF 'lands on the same default' switch.sh &amp;&amp; echo PASS</automated>
    <fails_when>A do-not-touch item (REQUIREMENTS SWITCH-03 line 18 or the switch.sh inline comment) was altered — D-10 scope was over-reached.</fails_when>
    <automated>! git -C /home/pengwin/repos/qwen38-27b-rtx3090 diff --name-only HEAD~1 HEAD -- .planning/STATE.md .claude/CLAUDE.md | grep -q . &amp;&amp; echo PASS</automated>
    <fails_when>The D-10 commit touched a GSD auto-mirror (STATE.md or .claude/CLAUDE.md) — mirrors resync from PROJECT.md; hand-editing them would diverge them from the source (WARNING-1 correction).</fails_when>
    <automated>git -C /home/pengwin/repos/qwen38-27b-rtx3090 log --oneline -1 | grep -qF 'align default-profile wording' &amp;&amp; git -C /home/pengwin/repos/qwen38-27b-rtx3090 show --stat HEAD | grep -qF 'PROJECT.md' &amp;&amp; git -C /home/pengwin/repos/qwen38-27b-rtx3090 show --stat HEAD | grep -qF 'switch.sh' &amp;&amp; ! git -C /home/pengwin/repos/qwen38-27b-rtx3090 show --stat HEAD | grep -qE '\.gsd/|\.current-profile' &amp;&amp; echo PASS</automated>
    <fails_when>The D-10 commit is missing, has a different subject, its --stat does not list the aligned doc files, or it stages a forbidden file (.gsd/ or .current-profile) (WARNING-2 negative assertion).</fails_when>
  </verify>
  <done>Exactly eight doc locations now state the `batch` default (REQUIREMENTS x2, ROADMAP x2, PROJECT x3 — Key Decisions row + Core Value + active-requirement, switch.sh header x1), matching switch.sh:109; the do-not-touch items are byte-identical (including the two real GSD mirrors, which the D-10 commit's diff is negative-checked against); and the D-10 commit is made staging only the four doc files, negatively verified to exclude .gsd/ and .current-profile, with nothing pushed.</done>
</task>

</tasks>

<trust_boundaries>
| Boundary | Description |
|----------|-------------|
| executor doc edits -&gt; qwen-repo files -&gt; GSD mirrors | the four edited doc files feed STATE.md / .claude/CLAUDE.md mirrors that resync on the next GSD state update; an over-broad edit would propagate a wrong default into the mirror |
| git index -&gt; qwen-repo history | the two commits rewrite the repo's documented history; staging a forbidden file (.gsd/, .current-profile) would leak runtime state or tooling into the tracked tree |
</trust_boundaries>

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Tampering | D-10 over-edit (altering a do-not-touch item) | medium | mitigate | The plan lists the exact eight before/after edits and an explicit do-not-touch list (SWITCH-03 line 18, switch.sh inline comment 97-100, the real GSD auto-mirrors STATE.md:25 + .claude/CLAUDE.md, .gsd/); the executor stops and reports if any before-string is missing, and the verifies confirm the do-not-touch items remain present/unchanged, including a negative diff check that the D-10 commit touched neither mirror. |
| T-03-07 | Denial of Service | GSD milestone state left inconsistent | low | mitigate | Task 1 commits the pending Phase 2 bookkeeping (SWITCH completion, phase-2 roadmap status, state.json, quick SUMMARY) before the D-10 edits, so the milestone state and the docs agree; the executor checks `git status --porcelain` first to stage only what is actually pending. |
| T-03-08 | Tampering | mixed file staged across the two commits | medium | mitigate | Ordering: Task 1 (phase-2 bookkeeping) commits before any D-10 edit, so each file's Phase-2 diff and D-10 diff land in separate commits; ROADMAP is excluded from Task 1 (already recorded in the planner's commit) so it carries only the D-10 diff in Task 2. |
| T-03-SC | Tampering | qwen-repo commit scope (staging forbidden files) | low | mitigate | The qwen repo has no pre-commit hooks (unlike the dotfiles repo), so the gate is explicit per-file `git add` of the named files plus a per-commit negative assertion in each task's verify: `! git show --stat HEAD | grep -qE '\.gsd/|\.current-profile'` (both commits — phase-2 bookkeeping and D-10); neither is ever staged by this plan. |

<verification>
- All eight D-10 edits are applied; the four files' targeted `single` references now read `batch`, matching switch.sh:109's actual default (including PROJECT.md's own Core Value + active-requirement source text, from which the GSD mirrors resync).
- The do-not-touch items (REQUIREMENTS SWITCH-03 line 18, switch.sh inline comment 97-100, the real GSD auto-mirrors STATE.md:25 + .claude/CLAUDE.md, .gsd/) are byte-identical to before (the D-10 commit's diff is negative-checked against both mirrors).
- Two clean qwen-repo commits exist in order: `docs(02)` (Phase-2 bookkeeping: REQUIREMENTS, state.json, quick SUMMARY) then `docs(03)` (D-10: REQUIREMENTS, ROADMAP, PROJECT, switch.sh); each stages only its named files; both carry a negative assertion that no .gsd/ or .current-profile is in the commit; nothing is pushed.
</verification>

<success_criteria>
- BOOT-02's documented default is now `batch` (matching the locked D-08 and the switch.sh:109 code) across all eight locations in REQUIREMENTS, ROADMAP, and PROJECT — including PROJECT.md's Core Value + active-requirement source text, from which the GSD mirrors (STATE.md:25, .claude/CLAUDE.md) resync automatically.
- The switch.sh header comment matches its code (both say `batch`), while its inline comment and the completed SWITCH-03 requirement remain untouched.
- The Phase 2 bookkeeping is committed as its own clean commit, and both commits are negatively verified to stage no .gsd/ or .current-profile — the GSD milestone state is consistent and the Phase 3 boot work proceeds on a clean, self-consistent record.
</success_criteria>

<output>
Create `.planning/phases/03-boot-autostart/03-02-SUMMARY.md` when done
</output>
